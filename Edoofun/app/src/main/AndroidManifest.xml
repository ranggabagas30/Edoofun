<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edoofun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pilihan_minat"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".sosial"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".inbox"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".pesan_baru"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".isi_pesan"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".postingan_baru"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".postingan_detail"
            android:label="@string/app_name">
        </activity>

        <activity android:name=".ForgotPass"/>

        <activity android:name=".Register"/>

        <activity android:name=".PembayaranKoin"/>

        <activity android:name=".KategoriDetail"/>

        <activity android:name=".HalamanHome"/>
    </application>

</manifest>